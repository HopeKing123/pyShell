可迭代的与迭代器：
# 哪些是可迭代的
1.生成器 2.字符串 3.列表 4.集合 5.字典都是可迭代的

# 判断对象是否是可迭代的
Iterable
isinstance(x,Iterable) ---> True False

# 迭代器
生成器就是一个迭代器 ---> next(g) ---> next(g) ---> 元素
列表是可迭代的但不是迭代器需要转换
next(list) ---> iter(list) ---> next(iter(list)) ---> 得到元素